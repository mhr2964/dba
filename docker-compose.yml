services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: dba
      POSTGRES_PASSWORD: dba
      POSTGRES_DB: dba
    ports:
      - "5434:5432"
    volumes:
      - dba_pgdata:/var/lib/postgresql/data

volumes:
  dba_pgdata:
