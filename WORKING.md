# WORKING — live-instance lockfile

Companion to `HANDOFF.md`. Write a line here before making non-trivial edits to this project, in case another instance is running concurrently. Remove your line on commit or when done touching the file, whichever comes first.

Format: `YYYY-MM-DD HH:MM  model-id  file(s)  what`

See `System/CLAUDE/multi-model.md` § WORKING.md lockfile for the full convention.

---
