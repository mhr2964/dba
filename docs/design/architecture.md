# Architecture — DBA

Layered-monolith Python/discord.py/PostgreSQL Discord bot simulating an NBA fantasy league.

## Layers

| Package | Role |
| --- | --- |
| `bot/` | Discord-facing layer: `cogs/` (16 slash-command groups), `embeds/` (18 embed builders), `ui/` (5 interactive views/modals), `client.py` (bot bootstrap) |
| `services/` | Business logic: 45 top-level modules + `personas/` (20, columnist voice/prompt definitions), `philosophies/` (9, coaching-bias role-assignment modifiers), `trade_signals/` (10, trade-context signal providers) |
| `data/` | Persistence: `db.py` (asyncpg pool), `repositories/` (25 modules, one per aggregate — the only place raw SQL should live), `seeds/`, cached rating/stat snapshots (`2k_ratings/`, `bdl_cache/`, `stats_ratings/`, `tendency_cache/`) |
| `core/` | Cross-cutting: `config.py` (fail-fast required env vars), `errors.py`, `logging.py` |
| `phase/` | Season-phase state machine: `states.py`, `transitions.py`, `guards.py`, `helpers.py` |
| `alembic/` | 45 migrations, linear `down_revision` chain from `001` through `045` |
| `tests/` | pytest + pytest-asyncio, 253 tests (242 passing, 10 `xfail`, 1 integration-marked skip as of 2026-07-22) |
| `scripts/` | Durable CLI utilities (seed builders, rating fetchers, `purge_dba.py`, `query_db.py`) — one-off diagnostic/backfill scripts are not kept here; they're disposable once their fix has shipped |

## Invariants

- **`services/` never imports `discord`.** As of 2026-07-22, 10 of the ~91 service files still do (`batch_sim_runner.py`, `cpu_trade_service.py`, `draft_service.py`, `fa_service.py`, `feedback_log.py`, `import_service.py`, `league_service.py`, `news_router.py`, `notifier_service.py`, `playoff_service.py`) plus the two announcer adapters where it's expected (`cpu_trade_announcer.py`, `sim_channel_announcer.py`). Business logic should return plain payload dataclasses; posting to Discord is a presentation concern. `grep -rn "^import discord\|^from discord" services/` is the check — not yet clean, enforced incrementally as Phase 2 extractions land.
- **`data/repositories/` owns all SQL.** Raw `pool.fetch*/execute` calls scattered through `services/` should move into a repository module as each is touched. `grep -rn "pool\.\(fetch\|fetchval\|fetchrow\|execute\)" services/` is the check — same incremental enforcement as above.
- **`Announcer` protocol** (`services/announcer_protocol.py`) is the seam between business logic and Discord posting: `post_embed(channel_key, EmbedData)`, `post_text(channel_key, content)`. `channel_key` is the same string already passed to `league_repo.get_channel(pool, league.id, channel_key)` (e.g. `"trade-block"`) — this formalizes an existing convention rather than inventing a new one. `EmbedData` is a plain dataclass (title/description/color/fields/footer/thumbnail_url/image_url) covering the field usage seen across current `discord.Embed(...)` call sites. Every extraction in Phase 2 that currently posts directly to a channel implements or consumes this instead of importing `discord` itself; concrete adapters (`cpu_trade_announcer.py`, `sim_channel_announcer.py`) are added as part of that split, not before.

These two invariants are the organizing principle behind the two-largest-files split (`services/cpu_trade_proposals.py`, 4,274 LOC — split 2026-07-22; `services/batch_sim_runner.py`, originally 3,653 LOC, now 792 LOC — split 2026-07-22) — see the re-architecture plan referenced in `HANDOFF.md` for the current split status, and `trade-logic-rules.md` for the trade-evaluator behavior those files implement.

## Split status

- **`cpu_trade_proposals.py` (4,274 LOC) — done, 2026-07-22.** Became 6 files: `trade_gates.py` (207 LOC, `_apply_final_trade_gates`), `trade_proposal_scoring.py` (538 LOC, pure scoring/routing helpers), `trade_return_builder.py` (520 LOC, return-package assembly), `trade_block_builder.py` (142 LOC, trade-block derivation), `cpu_trade_announcer.py` (102 LOC, the only file here allowed to `import discord` — implements `Announcer`), and `cpu_trade_proposal_runner.py` (2,891 LOC — the slimmed orchestration core: mode dispatch, candidate search, the CPU-to-CPU auto-approve transaction). Extraction was a byte-accurate line-slice, not a retype — zero behavior change, verified by full-suite re-run (242 passed/10 xfailed/1 skipped, unchanged) plus a direct import of all 6 modules. `cpu_trade_proposal_runner.py` is still 2,891 LOC by design: it retains `_run_incoming_first_for_team` (~1,480 LOC), which the plan explicitly calls out as currently zero-coverage — **do not refactor that function's internals without adding characterization tests first**; relocating its call-site imports (what this pass did) is safe, rewriting its logic is not.
- **`batch_sim_runner.py` (originally 3,653 LOC) — done, 2026-07-22.** Four new modules extracted:
  - `sim_persistence.py` (543 LOC): the 9 pure roster/lineup/role-cache helpers (`_ensure_lineup`, `_compute_team_ovr`, etc. — zero `discord`, confirmed by grep before moving), plus `_persist_game_result` and `_persist_injuries`. The latter two were **not** pure — they interleaved DB writes with inline `discord.Embed` construction (injury alerts, win-streak/record embeds) and had zero test coverage. Before touching them: wrote `tests/test_persist_injuries.py` (7 cases) and `tests/test_persist_game_result.py` (5 cases) as characterization tests against the *original* inline-embed code, using a recording `_FakeChannel`. All 12 passed against the original implementation. Then converted both functions to build `EmbedData`/`EmbedField` and post through `_BoundChannelAnnouncer` instead of constructing `discord.Embed` directly — the same 12 tests were re-run against the converted code and passed unchanged, byte-for-byte (embed title/description/field/footer text identical). One exception left as a direct `channel.send`: the all-time-record embed is built by the pre-existing `bot/embeds/sim_embeds.season_record_embed()` (an already-opaque `discord.Embed`-returning builder, same pattern used elsewhere in this codebase) — not something this module constructs itself, so routing it through the announcer wasn't necessary to satisfy the invariant.
  - `sim_channel_announcer.py` (126 LOC): the 6 channel-lookup helpers (`_get_box_scores_channel`, `_ensure_records_channel`, etc. — pure resolution logic, no characterization needed) plus `_BoundChannelAnnouncer`, a concrete `Announcer` that wraps an *already-resolved* channel object rather than resolving by `channel_key` per call (unlike `cpu_trade_announcer.py`). This deviates from the protocol's literal channel_key-lookup design — sim orchestration resolves each channel once per batch and threads the object through several function calls (`_sim_single_game` → `_persist_game_result` → `_persist_injuries`), and changing that threading architecture was out of scope for this pass. `channel_key` is accepted (for structural conformance) but ignored.
  Both new modules verified: ruff clean, import without circular-import errors (broke a `sim_persistence.py` ↔ `batch_sim_runner.py` cycle with a deferred local import of `_maybe_post_triage_report`, same pattern already used elsewhere in this codebase for cross-module calls), full suite green at 254 passed (242 + 12 new)/10 xfailed/1 skipped.
  - **`cpu_trade_round_trigger.py`** (470 LOC, new): `_maybe_run_cpu_trades`, `_run_cpu_trades_inner`, `_is_blockbuster_trade` — bridges sim batches to CPU trade rounds and announces the results (trade-executed/pending embed + thread, Marcus Cole blockbuster analysis). Same characterization-tests-first treatment: `tests/test_run_cpu_trades_inner.py` (7 cases, using recording fakes for the channel/message/thread objects and mocking `columnist_service.generate` + the deep context-signal chain rather than re-verifying trade-evaluation business logic) confirmed against the original inline-`discord.Embed` code first, then the conversion to `EmbedData`/`_BoundChannelAnnouncer` was verified against the same 7 tests unchanged. This conversion needed two new `_BoundChannelAnnouncer` methods beyond the base protocol: `post_embed_get_ref` (returns the sent message so `feedback_log` and thread-creation can still use it) and `create_thread_and_send` (wraps `message.create_thread(...)` + `thread.send(...)`) — threads aren't expressible in the base `Announcer.post_embed`/`post_text` shape, so this is a deliberate, documented extension rather than a protocol violation. `_interest_score_from_batch_result` (dead code — defined, never called anywhere in the codebase) and `_fetch_race_leaders_once` (used only by the still-untouched `_maybe_post_the_race` et al.) were deliberately left in `batch_sim_runner.py`, not part of this group.
  Full suite after this step: 261 passed (254 + 7 new)/10 xfailed/1 skipped. `batch_sim_runner.py` is now 2,682 LOC before the `sim_content_pipeline.py` work below began.
  - **`sim_content_pipeline.py`** (done, 2026-07-22, 1,974 LOC) — all 12 `_maybe_post_*` columnist/report functions moved, one at a time (each: characterization tests against the original → convert to `EmbedData` → re-verify → extract → commit): `_maybe_post_awards_races` (mechanical move, no conversion needed — it only calls the existing `bot/embeds/awards_embeds.awards_race_embed()` builder and forwards the result, same precedent as `sim_embeds.season_record_embed`), `_maybe_post_potm`, `_maybe_post_coach_beat` (the ~50-game cadence counter `_coach_beat_game_counter` moved with it), `_maybe_post_power_list` (the rank-delta-from-previous-article parsing and the ~70-game cadence counter `_power_list_game_counter` moved with it), `_maybe_post_rookie_watch` (~70-game counter `_rookie_watch_game_counter`), `_maybe_post_big_picture` (~70-game counter `_big_picture_game_counter`), `_maybe_post_ledger` (phase gate, force-fire-on-first-post-of-season, ~280-game counter/`_ledger_first_post_done`; brought its own `Phase` import), `_maybe_post_the_race` (~280-game counter `_race_game_counter`; `_fetch_race_leaders_once`, a separate batch-flush-site helper, stayed in `batch_sim_runner.py` since it isn't actually called by this function), `_maybe_post_triage_report` (this move **resolved the `sim_persistence.py` ↔ `batch_sim_runner.py` deferred-import Trap** — `sim_persistence.py`'s `_persist_injuries` now imports it from `sim_content_pipeline.py` as a normal top-level import, since `sim_content_pipeline.py` has no dependency back on `sim_persistence.py`), `_maybe_post_prelude` and `_maybe_post_playoff_columnist` (both functions' only callers live in `services/playoff_service.py`, each via a deferred `from services import batch_sim_runner as _bsr` local import used solely to reach that one function — all 5 call sites across the two functions were updated to `from services import sim_content_pipeline as _scp`; `_maybe_post_playoff_columnist` brought its own `_PLAYOFF_COLUMNIST_ROTATION`/`_playoff_rotation_index` state), and finally **`_maybe_post_columnist`** (the big one, ~825 LOC, the main article dispatcher — the most feedback-tuned part of the bot). `_maybe_post_columnist` has THREE independent posting paths in one function (the main rotation article including a `hot_take_hour` debate-format variant and Pat Chen strategy enrichment, an independent Darius Cole tank-watch column firing every ~30 games, and an independent Marcus Brooks power-rankings column firing every ~200 games) plus `columnist_ride_along` prompt-capture integration — extracted via mechanical byte-slice (827 lines, matching the precedent used for the original `cpu_trade_proposals.py` split) rather than manual retyping, to guarantee zero transcription drift on a function this large. `tests/test_maybe_post_columnist.py` (9 cases) covers the interesting-batch gate (blowout/streak/big-game/trade/50-game-fallback), the force-mode suppression gate, the rotation index still advancing on a skipped or re-rolled slot, the hot_take_hour DAVE/TONY bold-replacement, and both independent counters firing at threshold — not full line coverage of every enrichment branch (Pat Chen strategy data, HTH season-narrative seeding, award-race hooks are exercised so they don't crash, but not asserted on in detail), matching the characterization depth used throughout this migration. Brought its own module state (`_marcus_game_counter`, `_darius_game_counter`, `_last_columnist_game_index`, `_COLUMNIST_ROTATION`, `_columnist_rotation_index`, `_HTH_NARRATIVES`, `_COLUMNIST_FORCE_MIN_GAP`) and new imports (`strategy_service`, `_player_style_context`, `_columnist_ride_along`, `Counter`) — all now dead in `batch_sim_runner.py` and removed, along with `league_repo`, `columnist_service`, `_PERSONAS`, and `_feedback_log`, which turned out to have zero remaining consumers there once this function left (every other caller had already migrated in earlier parts). Also removed a genuinely dead leftover: a duplicate `_potm_last_checked_month` dict in `batch_sim_runner.py` that ruff never flagged (unused module-level variables aren't caught by the standard lint rules, only unused imports) — found by inspection while cleaning up this block. `_PERSONA_COLORS`/`_rgb_to_int` are consolidated now: `batch_sim_runner.py`'s copy is gone, `sim_content_pipeline.py`'s is the only one. **`batch_sim_runner.py` is now 792 LOC** (down from the original 3,653) — under the ~800 LOC target from the re-architecture plan.

## Phase 3 splits (opportunistic, incremental — not part of the Phase 2 god-file work above)

- **`trade_evaluator.py` (1,819 LOC) — done, 2026-07-22.** Split into 5 files by category, all pure relocation (zero behavior change, verified by full-suite re-run at 320 passed, unchanged from before the split): `trade_value_math.py` (591 LOC — pure player/pick valuation math: `player_trade_value`, `player_team_specific_value`, `pick_trade_value`, `apply_form`, `_age_multiplier`, `_upside_modifier`, `defensive_impact_modifier`, `_contract_modifier`, `experience_premium`, `_effective_fleecing_floor`, no DB/async), `trade_context_builder.py` (426 LOC — DB-touching: `compute_form_map`, `compute_team_mode`, `build_team_context`, plus their caches), `trade_grading.py` (193 LOC — `evaluate_trade`, `grade_trade`, `_player_archetype`, `_team_primary_scheme`), `cpu_trade_acceptance.py` (605 LOC — `cpu_should_accept`, the B1/B3/B5/B6/B7/B8 gate rules, `LEAD_ROLES`), and `trade_ai_reasoning.py` (75 LOC — `get_ai_reasoning`, fully standalone LLM call). Extracted via AST-based segment extraction (`ast.get_source_segment` per top-level def/assignment, reassembled by category) rather than manual line-slicing, since the original file's constants/functions interleaved across target modules (e.g. the form-cache block sat between two different destination modules) in a way that made a single contiguous byte-slice impossible. This turned out to be the highest-blast-radius split of the whole re-architecture: ~40 call sites across 11 other files (`cpu_block_service.py`, `cpu_trade_posture.py`, `cpu_trade_proposal_runner.py` alone had 23 call sites, `franchise_plan_service.py`, `ra_reasoning.py`, `team_intel.py` ×2 deferred-import sites, `trade_gates.py`, `trade_proposal_scoring.py`, `trade_return_builder.py`, `trade_service.py`, `bot/cogs/trade_cog.py`) plus 8 test files, all updated to import the correct new module. `tests/test_trade_evaluator.py` (31 cases testing 3 different concerns under one misleading name) was split into `test_trade_value_math.py`, `test_trade_grading.py`, and `test_cpu_trade_acceptance.py` to match the new module boundaries — same naming-convention rule applied throughout this codebase ("a name that misrepresents its subject gets fixed, not noted"). One franchise_plan_service.py import alias (`_trade_eval`) was also renamed since it became misleading after the split.
- **`ra_reasoning.py` (1,384 LOC) — done, 2026-07-22.** Split into 3 files, zero external blast radius: of the 3 files that call into this module (`cpu_trade_proposal_runner.py`, `team_intel.py` — comment-only reference, `trade_service.py`), all of them only ever call the two top-level public entry points (`render_trade_panel`, `pick_ids_to_dicts`), both of which stayed in the slimmed `ra_reasoning.py` (625 LOC) — so no external file needed an import change. Split: `trade_narrative_lines.py` (506 LOC — pure sentence-builder functions, no DB/async: `_window_line`, `_motivation_incoming`, `_motivation_outgoing`, `_role_fit_line`, `_defense_quality_line`, `_window_fit_line_incoming/outgoing`, `_scheme_implication_line`, etc.) and `trade_reasoning_fetchers.py` (302 LOC — DB-touching per-player/pick lookups: `_project_pick_slot`, `_fetch_player_full`, `_fetch_position_depth`, `_fetch_roster_median_ovr`, `_fetch_player_form`, `_synergy_line`). Same AST-based extraction method as the `trade_evaluator.py` split. Since this was genuinely untested code (zero coverage, no direct or indirect test exercised any of these functions), wrote `tests/test_trade_narrative_lines.py` (47 cases covering every pure sentence-builder function's main branches) BEFORE moving anything — all 47 passed against the original `ra_reasoning.py`, then unchanged after the split. Full suite: 367 passed (320 + 47 new)/10 xfailed/1 skipped.
- **`franchise_plan_service.py` (1,439 LOC) — done, 2026-07-22.** Same zero-external-blast-radius pattern as `ra_reasoning.py`: every external caller (`scripts/derive_franchise_plans.py`, `batch_sim_runner.py`, `ra_reasoning.py`, `role_service.py` — comment only, `team_intel.py`, `trade_block_builder.py`, `trade_service.py`) only ever calls `derive_and_persist_all`, `get_or_derive`, or `get_plan` — all 3 of which stayed in the slimmed `franchise_plan_service.py` (507 LOC), so no external file needed an import change. Split: `franchise_plan_math.py` (706 LOC — pure decision/classification logic: `_derive_goal_and_horizon`, `_categorise_players`, `_should_pivot`, `_is_reassessment_checkpoint`, `_build_rationale`, `_production_tier`/`_defensive_tier`/`_combined_tier`) and `franchise_plan_production.py` (281 LOC — current-season production stat fetching with a prior-season BDL cache fallback). Same AST-based extraction method as the other two Phase 3 splits. `_derive_goal_and_horizon` already had dedicated coverage (`tests/test_franchise_plan_early_season.py`, 5 cases, import retargeted); everything else was untested, so wrote `tests/test_franchise_plan_math.py` (37 cases covering `_should_pivot`, `_is_reassessment_checkpoint`, `_build_rationale`, the tier classifiers, and `_categorise_players`'s per-goal branches) BEFORE moving anything — all 37 passed against the original code (one test had a wrong expected-value guess for the archetype-bump case, corrected to match actual behavior before counting as green), then unchanged after the split. Full suite: 404 passed (367 + 37 new)/10 xfailed/1 skipped.
- **`trade_service.py` (1,352 LOC) — done, 2026-07-22.** Unlike the prior 3 splits, this file is DB-transactional orchestration end to end (no pure decision-math layer) — 28 external call sites, but every one of them (`bot/cogs/trade_cog.py`, `cpu_trade_proposal_runner.py`, `cpu_trade_posture.py` — comment only, `scripts/headless_test.py` — unrelated `cpu_trade_service` reference, `tests/conftest.py`, `tests/test_apply_final_trade_gates.py`) only ever calls `propose`, `accept`, `decline`, `approve`, `veto`, or `get_pending_queue` — the 6 public functions, all of which stayed in the slimmed `trade_service.py` (541 LOC). Split out `cpu_trade_evaluation.py` (851 LOC — `_compute_live_mode`, `_cpu_evaluate`, `_attempt_counter_offer`: the CPU-side accept/counter/reject decision, called only internally by `propose`). Found and fixed a genuine circular-dependency risk during the split: `_attempt_counter_offer` calls back into `create_trade_record` (which stayed in `trade_service.py`), while `trade_service.py`'s `propose` calls `cpu_trade_evaluation._cpu_evaluate` — resolved with a deferred local import of `create_trade_record` inside `_attempt_counter_offer`, the same pattern already used elsewhere in this codebase (`_get_trade_context` in `cpu_trade_acceptance.py`, the `role_service` import in `franchise_plan_service.py`). Unlike the previous 3 Phase 3 splits, no new characterization tests were written here — this is DB-heavy orchestration glue (not pure decision logic), the AST-based extraction guarantees byte-identical function bodies, and the downstream calls it makes (`cpu_should_accept`, `evaluate_trade`, `compute_form_map`, `build_team_context`) are already covered by their own tests from the earlier splits. Full suite: 404 passed/10 xfailed/1 skipped — unchanged count, confirming the pure relocation didn't break anything. `cpu_trade_evaluation.py` at 851 LOC is slightly over the ~800 soft target; further splitting `_cpu_evaluate`/`_attempt_counter_offer` apart would fragment a tightly-coupled decision flow for no real benefit, so left as one cohesive module.
- **`role_service.py` (1,163 LOC) — done, 2026-07-22.** Split into `role_scoring.py` (658 LOC — `ROLE_REGISTRY` the 23-entry role taxonomy, `_score_role_fit`, `_derive_tendency_respecter`: the pure greedy role-assignment algorithm) and a slimmed `role_service.py` (548 LOC — DB orchestration: `derive_roles`, `persist_roles`, `get_or_derive_roles`, `derive_and_persist_all[_for_team]`, `assert_role_constraint_sync`). `bot/cogs/coach_cog.py` accesses `role_service.ROLE_REGISTRY` directly (not through a function call), so `ROLE_REGISTRY` is re-imported into the slimmed `role_service.py` — same re-export pattern as `ra_reasoning.py`'s `_compute_posture` — keeping that external access working with zero call-site changes. `alembic/versions/041_role_check.py`/`042_coach_philosophy_check.py` only reference `ROLE_REGISTRY`/`COACH_PHILOSOPHIES` in comments (not imports), so nothing to update there. Zero coverage existed for `_score_role_fit`/`_derive_tendency_respecter` despite being the core of CPU role assignment, so wrote `tests/test_role_scoring.py` (15 cases) BEFORE moving anything — one test's assumption was wrong on the first run (expected "exactly one primary-category role" but `post_anchor` is deliberately BOTH a primary-scoring role and a defensive-anchor role by design, so a team can get one via each path independently; corrected the test to check the real invariant — exactly one *guard/wing* primary — before counting it as green). Full suite: 419 passed (404 + 15 new)/10 xfailed/1 skipped.
- **`columnist_service.py` (1,531 LOC) — done, 2026-07-22, the last Phase 3 file-split target.** Despite being flagged as the worst blast-radius candidate (17 files reference it), every single one of those 17 files plus all 12 indirect `test_maybe_post_*.py`/`test_run_cpu_trades_inner.py` tests only ever call `columnist_service.generate` — the one function that had to stay regardless. Split into `columnist_assembly.py` (856 LOC — the 12 `_assemble_*` format-style renderers, the 3 `[SENTINEL]`-based body parsers, and `_assemble_article`, the dispatcher) and a slimmed `columnist_service.py` (712 LOC — `ColumnistRequest`, `_tolerant_json_parse`, `_is_refusal`, `_format_signals_block`, `generate()`). Zero coverage existed for any of the assembly/parsing logic, so wrote `tests/test_columnist_assembly.py` (37 cases, one per renderer/parser's main shape) BEFORE moving anything — 3 tests had a wrong assumption about `_dedupe_headline`'s exact behavior (assumed the stripped remainder kept its leading blank lines; it doesn't, since the real code calls `.lstrip("\n")` on it) caught and corrected against the original before counting as green. The AST-extraction script has a real gap discovered here: it doesn't capture decorator lines (`@dataclass` above `class ColumnistRequest`) since the leading-comment scanner only recognizes `#`-prefixed lines, not `@`-prefixed ones — caught by a failed import, fixed manually, worth checking for on every future AST-based split that moves a decorated class/function. Also found and fixed 5 PRE-EXISTING ruff violations in the moved code (2× `E741` ambiguous variable name `l`, 3× `E701` multiple statements on one line) that predated this session entirely — confirmed via `git show HEAD:services/columnist_service.py` before fixing, so they weren't mistaken for something this split introduced. Full suite: 456 passed (419 + 37 new)/10 xfailed/1 skipped. **All Phase 3 file-level splits are now complete.**
- **Remaining, lower priority, incremental, do as normal feature work touches them:** oversized cogs (`roster_cog.py` 827). Same Category 11 split + "cogs dispatch only" rule applies. (`trade_cog.py`, `coach_cog.py`, `setup_cog.py`, `sim_cog.py`, and `admin_cog.py` are done — see "Cog splits" below.)

## Cog splits (opportunistic, incremental — lower priority than Phase 3, near-zero test coverage)

Cogs can't be verified with characterization tests or a full pytest re-run the way the Phase 2/3 service splits were — there's no way to start a live Discord bot and exercise command registration/dispatch in this environment. Per explicit user direction, verification here is **import-check + `ruff check` + manual review only**, a deliberately weaker bar than the service splits above. Extraction method is also different: whole `app_commands.Group`/`discord.ui.View` classes move as a unit via direct line-slicing (unambiguous class boundaries), not AST-based category reassembly.

- **`trade_cog.py` (1,239 LOC) — done, 2026-07-22.** Split into `bot/cogs/trade_block_cog.py` (360 LOC — `TradeBlockGroup`, the `/block add|remove|view|league` commands, registered as its own `TradeBlockCog` extension) and `bot/ui/trade_views.py` (47 LOC — `TradeAcceptView`, matching the existing `box_score_views.py`/`draft_views.py`/`stats_views.py` convention for `discord.ui.View` subclasses), leaving a slimmed `bot/cogs/trade_cog.py` (875 LOC — `TradeGroup`, the `/trade propose|accept|decline|approve|veto|list` commands, plus `_parse_ids`/`_build_asset_lookup`). `TradeBlockGroup` had zero dependency on any `TradeGroup`-only import, so this was a clean unit extraction. Registered the new cog in `bot/client.py`'s explicit extension list (not auto-discovery) alongside `"bot.cogs.trade_cog"`. Removing `TradeBlockGroup` left `trade_block_repo` and `datetime` unused in `trade_cog.py` — both removed (the latter is a genuinely new-from-this-split unused import, not pre-existing, so it was safe to remove without a `git show HEAD` provenance check). Verified via `python -c "import bot.cogs.trade_cog; import bot.cogs.trade_block_cog; import bot.ui.trade_views; import bot.client"` and `ruff check` on all 4 touched/created files — both clean. No test suite coverage exists for either cog (not a regression — none existed before this split either). Two stray inline `discord.Embed(...)` constructions remain in `trade_cog.py`/were also present in the extracted `trade_block_cog.py` (in `TradeGroup.list` and `TradeBlockGroup.remove`) that could still move into `bot/embeds/trade_embeds.py` to match convention — left as-is, lower priority than the split itself.
- **`coach_cog.py` (1,223 LOC) — done, 2026-07-22.** Structurally different from `trade_cog.py`: `RoleGroup`, `DirectiveSubGroup`, and `PhilosophyGroup` aren't independent top-level command groups — `CoachGroup.__init__` composes all three as subcommands of one `/coach` tree (`self.add_command(RoleGroup())` etc.), and the legacy `/coach assign-role`/`show-roles`/`unlock` aliases call directly into the same `_do_role_*` helper functions `RoleGroup`'s commands use. That coupling means this wasn't a "move a whole self-contained class" split like `trade_block_cog.py` — it required caller-footprint analysis of every module-level helper first (which functions/constants each of the 5 top-level classes actually touches) to find the real seam. Found two clusters: a **role cluster** (`_ROLE_CHOICES`, `_get_season`, `_resolve_caller_team`, `_is_commissioner`, `_resolve_target_team`, `_resolve_player_on_team`, `_player_autocomplete_for_team`/`_player_autocomplete_own_team`, `_roles_embed`/`_assign_confirm_embed`/`_unlock_confirm_embed`, `_do_role_assign`/`_do_role_show`/`_do_role_unlock`) used by both `RoleGroup` and `CoachGroup`'s legacy aliases — this stayed in `coach_cog.py` (slimmed to 729 LOC) since the two are tightly coupled — and a **directive cluster** (`_VALID_VALUES`, `_CATEGORY_COLUMN`, `_CATEGORY_LABELS`, `_get_league_id`, `_require_manager_or_commissioner_directive`, `_resolve_team_for_directive_view`) used only by `DirectiveSubGroup`, with zero overlap with the role cluster. `PhilosophyGroup` had zero dependency on any pre-existing module-level helper — a clean unit move. Both moved to a new `bot/cogs/coach_directive.py` (474 LOC) along with `_DirectiveLegacyGroup` (the `/directive` top-level alias group), which is NOT itself a registered extension — `coach_cog.py` still imports and registers all three from there, same command-tree shape as before, this is pure code reorganization. One genuinely cross-cutting piece existed: `_send_deprecation_warning`/`_DEPRECATION_WARNED`, used by both `CoachGroup`'s legacy aliases (role cluster, stayed in `coach_cog.py`) and `_DirectiveLegacyGroup` (directive cluster, moved out) — rather than force one file to import from the other (risking a two-way circular import once `coach_cog.py` also needs to import `DirectiveSubGroup`/`PhilosophyGroup`/`_DirectiveLegacyGroup` from the directive module), it was extracted to a third tiny module `bot/cogs/coach_common.py` (38 LOC) that both import from — a one-directional dependency graph (`coach_cog.py` → `coach_directive.py`, both → `coach_common.py`), no cycle. `_DirectiveLegacyGroup`'s methods type-annotate a local variable as `coach_group: CoachGroup` (a type only defined in `coach_cog.py`) — safe without importing it at runtime because the file has `from __future__ import annotations` (PEP 563 lazy annotations), but imported under `if TYPE_CHECKING:` anyway for static-checker clarity, at zero runtime cost. Verified via `python -c "import bot.cogs.coach_cog; import bot.cogs.coach_directive; import bot.cogs.coach_common; import bot.client"`, `ruff check` on all 3 files (clean), and instantiating `CoachGroup()` directly + walking its command tree to confirm all 11 expected command names (`role`, `directive`, `philosophy`, `assign`, `show`, `unlock`, `set`, `view`, `reset`, plus the 3 legacy flat aliases) are still registered — the closest thing to a live-bot check available in this environment. `bot/client.py` needed no changes — `CoachCog` was and still is the only registered extension for this command tree.

- **`setup_cog.py` (1,015 LOC) — done, 2026-07-22.** Same shape as `trade_cog.py`: `LeagueGroup` and `TeamGroup` are two genuinely independent top-level command groups (zero cross-references either direction, confirmed by grep before extracting), plus a `SetupCog.help` command and its `_COMMAND_GROUP_PHASES` phase-availability table which depend on neither group. Split `TeamGroup` out into a new `bot/cogs/team_cog.py` (477 LOC), registered as its own `TeamCog` extension in `bot/client.py`, leaving a slimmed `setup_cog.py` (569 LOC — `LeagueGroup`, `_supported_seasons`/`_DATA_ROOT`, `_COMMAND_GROUP_PHASES`, `SetupCog`, `setup()`). Removing `TeamGroup` left `typing.Optional` and `services.team_intel` unused in `setup_cog.py` — both were genuinely new-from-this-split (confirmed by ruff, not pre-existing) and removed. **Unlike `trade_cog.py` and `coach_cog.py`, this file has real (if currently xfailed) test coverage** — `tests/test_setup_cog.py` imports `TeamGroup` directly and uses `mock.patch("bot.cogs.setup_cog....")` string targets for both `LeagueGroup`- and `TeamGroup`-related tests. Updated the import to pull `TeamGroup` from the new module and retargeted the 4 `TeamGroup`-test `mock.patch` strings from `bot.cogs.setup_cog.*` to `bot.cogs.team_cog.*` (patch targets follow the name's new lookup location, same rule as every other split in this migration). Also added a `bot.cogs.team_cog.get_pool` patch to `tests/conftest.py`'s autouse `patch_get_pool` fixture, alongside the pre-existing `bot.cogs.setup_cog.get_pool` one, since `TeamGroup`'s `get_pool` import is now a separate module-level name. Ran `tests/test_setup_cog.py` directly after the fix: all 8 tests still `XFAIL` for the exact same pre-existing reason (`core/errors.py:36`'s `age_ms > 1500` MagicMock-vs-int compare, unrelated to this split) — confirms the split didn't turn a known xfail into a new collection error. Full suite re-run to confirm no other regressions.

- **`sim_cog.py` (910 LOC) — done, 2026-07-22.** `SimGroup` (the `/sim run|to-deadline|to-next-rival|...` command tree, still 458 LOC after its own internal deprecation aliases) shares zero module-level helpers with 5 flat, standalone `@app_commands.command`-decorated legacy top-level commands (`/ready`, `/forceready`, `/standings`, `/schedule`, `/box-score`) that predate the `/team`/`/admin`/`/season` groups those commands now live under — confirmed by caller-footprint grep before extracting. Moved the 5 legacy commands plus their own `_send_top_dep_warning`/`_TOP_LEVEL_DEP_WARNED` deprecation-notice pair into a new `bot/cogs/sim_legacy_aliases.py` (289 LOC) — a companion module, not a separate `bot/client.py` extension, since `SimCog.__init__` still registers each of the 5 individually (`self.bot.tree.add_command(ready_command)` etc.) exactly as before. **Also found and removed 136 lines of genuinely dead code** while doing the caller-footprint pass: `_auto_sim_and_advance` and `_batch_advance` (both full async functions, defined but never called anywhere in the codebase — confirmed via a repo-wide grep, no dynamic dispatch or string-based lookup references them either) sat between the legacy commands and `SimCog`, apparently superseded by logic that moved elsewhere without these two being cleaned up. Same category of finding as the `_interest_score_from_batch_result` dead function caught during the `cpu_trade_round_trigger.py` split (Phase 2 part 4) and the `_potm_last_checked_month` dict caught during Phase 2 part 15 — ruff doesn't flag unused module-level functions, only unused imports, so this requires a manual repo-wide grep to catch. `sim_cog.py` is now 473 LOC (from 910); removing the legacy block and `box_score_summary_embed`/`box_score_team_embed`/`BoxScoreView`/`team_repo`/`typing.Optional` imports (all now unused in the slimmed file, confirmed via ruff) left it clean. No test suite coverage exists for either file (confirmed via `grep -rl sim_cog tests/` before starting — matches `trade_cog.py`/`coach_cog.py`, not `setup_cog.py`). Verified via `python -c "import bot.cogs.sim_cog; import bot.cogs.sim_legacy_aliases; import bot.client"`, `ruff check` (clean), and directly instantiating `SimGroup(MagicMock())` plus checking all 5 legacy command objects' `.name` attributes to confirm the full command surface (13 names total) survived the split.

- **`admin_cog.py` (866 LOC) — done, 2026-07-22.** A new shape not seen in the first 4 cog splits: `AdminGroup` is ONE class holding all 10 `/admin` subcommands as methods — no independent top-level groups (`trade_cog.py`/`setup_cog.py`'s shape) and no composed subgroups (`coach_cog.py`'s shape). None of the 10 command methods reference `self` for anything beyond the implicit binding (confirmed by grep — zero `self.` usage outside `AdminCog.__init__`'s `self.bot`), which made a different mechanical pattern viable: convert the 6 largest/most substantial commands (`edit-player`, `rollback-game`, `recalculate-standings`, `force-fa-sign`, `reseed-rosters`, `purge-server` — the commissioner data-correction/recovery tools, ~606 combined LOC) into standalone `app_commands.Command` objects (drop `self`, keep everything else byte-identical) in a new `bot/cogs/admin_data_ops.py` (679 LOC, including the 3 module-level consts `_PLAYER_FIELD_ALLOWLIST`/`_FIELD_TO_COLUMN`/`_STRING_FIELDS` that only `edit-player` used), then attach them to `AdminGroup` via `self.add_command(edit_player)` etc. inside a new `AdminGroup.__init__`. This is the same `Group.add_command()` API already used throughout this codebase for composing whole `Group` objects (`CoachGroup.__init__` → `RoleGroup()`/`DirectiveSubGroup()`/`PhilosophyGroup()`) — it works identically for standalone `Command` objects, and a custom `Group.__init__` calling `super().__init__()` + `add_command()` doesn't interfere with the remaining 4 commands still defined as class-body methods (`ensure-channels`, `rebuild-lineups`, `force-ready`, `restart`) since those register via discord.py's metaclass at class-definition time, independent of `__init__` — verified by directly instantiating `AdminGroup()` and confirming all 10 subcommand names are present. `admin_cog.py` is now 241 LOC (from 866); `admin_data_ops.py` holds the other 679. No test coverage exists for either file. Verified via import-check, `ruff check` (clean except `admin_cog.py`'s pre-existing unused `asyncio` import, confirmed via `git show HEAD` to predate this session), and the `AdminGroup()` instantiation + command-name check described above. `bot/client.py` needed no changes — `admin_data_ops.py` is a companion module, not a separate extension.

## Known non-architecture debt

- `README.md` previously claimed a `jobs/` and `notifier/` package split that never existed as anything but empty stub packages (removed 2026-07-22) — notifier logic lives in `services/notifier_service.py`.
