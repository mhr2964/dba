# HANDOFF — dba

```yaml
last-model: claude-sonnet-5
last-session: 2026-07-24
state: green
```

## Next action

**Columnist overhaul (both phases) is fully merged to `master`.** One item was deliberately left undone rather than force-fit: **B1's remaining renderer assignment** — none of the 5 revived `EmbedField` renderers (`_assemble_moment`/`_assemble_verdict`/`_assemble_index`/`_assemble_recap`/`_assemble_tactical` in `services/columnist_assembly.py`) got assigned to a persona beyond what Phase 1 already wired. Real structural blocker, not a skip: those 5 renderers consume the raw LLM `parsed` dict, which never leaves `columnist_service.generate()` today — callers only ever see `{"headline": str, "body": str}`. Assigning one needs new `EmbedData` dispatch inside `generate()`/`_assemble_article`'s call site itself. Worth a small dedicated follow-up if the dynamic-layout push continues (candidates: `carla_knox`, `hot_take_hour`, `jordan_rivera`, `keisha_williams`, `the_prelude` — `big_picture`/`marcus_cole` already got their own bespoke layout treatment instead).

Current test baseline: **658 passed, 1 skipped, 10 xfailed** (up from 577 pre-columnist-overhaul). Docker Postgres deadlock flakiness (see Traps) reproduced again during this work under 2 concurrent worktree agents — same non-code-related root cause as before, not a regression.

## Traps

- **Docker Postgres (`dba-db-1`) shows real flakiness under concurrent/heavy test load** — deadlocks on the `clean_db` autouse fixture's `TRUNCATE ... CASCADE` colliding with in-flight test inserts, and the container itself crashed/restarted more than once during this session's work. Confirmed via `git stash` A/B testing by two independent agents that this is NOT caused by any code change — it reproduces on unmodified files too. Root cause is likely the sandboxed Docker volume's WAL-replay speed combined with 3 pytest sessions hitting the same container concurrently (this session ran 3 parallel git-worktree agents against one shared DB) — a single sequential test run is clean (577 passed, 1 skipped, 10 xfailed, confirmed after all merges). Worth a real look (e.g. faster volume, or serializing `clean_db`) if concurrent test runs against this DB become routine.
- **`docs/design/trade-logic-rules.md` now has a B9 rule** (roster-hole downweight, the audit's Trade #4) — read it before touching trade proposal/acceptance logic again; B8's status line was also corrected (it was already shipped, the doc previously said otherwise).
- **10 pre-existing test failures are quarantined** via `@pytest.mark.xfail(strict=False)` (`test_setup_cog.py` ×8, `test_cpu_trade_acceptance.py` ×2). Full suite baseline is now 577 passed, 1 skipped, 10 xfailed (was 456 before this session's 121 new tests). A regression shows up as a NEW failure, not hidden inside these.

## Do not touch

- None currently.

## Recent context

- 2026-07-24 (latest): Shipped columnist overhaul **Phase 2** (per-persona content passes) — split into two parallel worktree agents by file ownership rather than the plan's original category grouping, after finding several items shared files in ways that would've collided (the per-persona word-target table in `columnist_service.py`, and Big Picture/Marcus Cole both needing output-shape changes that overlapped with the shared rendering plumbing). **Group A** (`d2db86c`, merged `--no-ff`): Pat Chen ships the previously-scoped-but-never-shipped ≤120-word hard cap plus player-detail rules (name the defender who got cooked, name the teammate who benefited), and a conditional history rule (real historical comparisons allowed when `all_time_records` data actually supports one, season-1 humility otherwise). Coach Beat gets a tightened length cap and a new scheme-history awareness (has this team run this defensive/offensive look before, did it work — new `gameplan_repo.py` query + `columnist_intel` provider). Marcus Brooks and Ren Takahashi — the two thinnest personas — get real analytical mechanics. Darius Cole gets real draft/prospect scouting substance. Power List and The Ledger get season-transition callbacks gated on real `season_history` data. **Group B** (`021d792`, merged `--no-ff`, one adjacent-line conflict in `_PERSONA_WORD_TARGET` resolved cleanly): Big Picture's word target now actually matches its own stated char cap (was internally inconsistent), gains real season-history/HOF callbacks, and gets a "Read full column" expand button (`bot/ui/big_picture_view.py`, new — short teaser by default, full column behind a button). Marcus Cole's trade reports reference real trade-magnitude rankings and split into two embeds (reporter-take summary + structured asset-breakdown grid) instead of one crammed description. Both agents got killed mid-run by a monthly Claude API spend cap partway through — Group A had already finished cleanly by then; Group B was resumed via `SendMessage` (not restarted fresh) once the user raised the limit, preserving its in-progress context. Independently verified both post-merge exactly like Phase 1 (ruff clean, full suite fresh in each worktree before merging, one more full run on `master` after both landed — 658 passed/1 skipped/10 xfailed, zero regressions). Full plan: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`.
- 2026-07-24 (later): Shipped columnist overhaul **Phase 1** (`1b2a1e9`, merged from an isolated worktree, `--no-ff`) — foundational infra for the concision/layout/history-flavor request that came out of watching the realism audit live. Per-format `max_tokens` budgets replacing the flat 1400 ceiling, `narrative_rule` scoped to long-form categories only, code-level length enforcement with a regenerate-once retry (mirrors the fact-check retry shape), tightened Discord description truncation (was a flat 2000-char slice), 5 previously-dead renderers revived as `EmbedField`-based layouts, 5 list-style personas (Power List/The Ledger/The Race/Triage Report/Rookie Watch) converted to multi-field embeds, a trade-magnitude comparator (`services/trade_magnitude.py`, new file), and 3 new `columnist_intel` providers (`season_history`/`hall_of_fame`/`all_time_records`, zero new schema). Independently verified post-merge (not just the agent's self-report): `ruff check` clean on all 7 touched files, full suite run fresh in the worktree — 625 passed/1 skipped/10 xfailed (636 collected, 48 new tests), then re-run on `master` post-merge with the same result modulo one confirmed-flaky unrelated test (`test_progression.py::test_high_potential_grows_more`, passes standalone, not touched by this work). Full plan: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`.
- 2026-07-24: Shipped a full realism/immersion audit — 23 findings across 3 domains, each researched by a dedicated Explore agent then fixed by a dedicated builder agent in an isolated git worktree (parallel dispatch, sequential merge, zero conflicts since domains touched disjoint files). **Trades** (11 fixes, `3ade9c3`): B6 archetype-redundancy now checked on trade *accept* not just search, positional-need gating for non-contenders, roster-shape floor, a new B9 roster-hole downweight rule, B3 upside modifier now reaches grading, season-long trade-partner cooldown, controlled variety in mode selection, star-power value premium, de-duplicated B1 want-check logic, `is_cornerstone` now uses live posture. **Scheme/personnel fit** (5 fixes, `fec6882`): CPU coaching posture now reads `franchise_plan.goal` (ports the B7 fix that only covered trades before), defensive scheme selection (press/switch_all) now gated on the team's own roster speed/defense, scheme tendency bumps now skill-conditioned per player instead of flat, roles and schemes now cross-reference (two-sided synergy bonus/penalty, no more forcing a center into a ball-handler role), bench role assignment nudges away from duplicate archetypes. **Columnist content** (7 fixes, `f5a5a84`): LLM-generated articles get a post-generation grounding check against real context data, player-level "declining/washed" claims are now grounded in real form data (reused the trade system's `compute_form_map`), Hot Take Hour's season narratives re-validate against current standings instead of freezing forever, Darius Cole's lottery odds are computed from real standings gaps instead of a hardcoded array, power rankings store structured rank data instead of regex-parsing prose, rookie stats are recency-weighted, and a documented-but-never-shipped Pat Chen prompt fix finally shipped. Full plan/findings detail: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md` (outside this repo).
- 2026-07-24 (earlier): closed out the prior re-architecture sweep fully — Discord intent toggle and all 3 trade verifications turned out already resolved, `dba-site` command reference pushed live, `sim_orchestrator.py` split shipped. See git log for that work.
