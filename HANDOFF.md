# HANDOFF — dba

```yaml
last-model: claude-sonnet-5
last-session: 2026-07-25
state: green
```

## Next action

**Realism sweep 4 (Season Rollover, Plan A) and sweep 5 (In-Game Strategy/Coaching AI, Plan B) are both fully merged to `master`. There is no Plan C queued.** The four-domain realism sweep initiative begun with sweep 1 (trades/scheme) is now COMPLETE across all five domains: trades/scheme, FA/draft/progression, playoffs/awards/HOF, season rollover, and now coaching AI. Full plan (both A and B): `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`. Absent a new user-directed initiative, there is no standing next action for this project.

**Plan B (coaching AI) shipped:** single-agent sequential build (not a parallel worktree dispatch — small enough domain, dependencies between rules mattered). CA1, CA2+CA3 (shipped together — see below), CA4, CA5, CA6, CA7 shipped; CA8 (foul-out mechanic), CA9 (same-game injury timing), CA10 (flat defensive matchups) deferred as documented decisions. See `docs/design/coaching-ai-logic-rules.md` (new) for full rule-by-rule detail — this is also the first design doc for this domain; it retroactively documents a PRIOR, undocumented realism pass ("Finding #1"-"Finding #5", visible only as inline code comments before this sweep).

**Headline fix: CA2+CA3 (shipped together — shipping either alone would have done nothing).** `sim_engine._build_box_for_team`'s role-based touch-share branch never read `usage_weight`/`star_usage_mult`, even though both were computed upstream (a "feature" directive's ×1.4 `usage_weight` multiplier had zero effect on scoring share) — that's CA2. Separately, `cpu_coach_service._decide_player_directives` had a redundant second `if posture == "tanking":` block that unconditionally overwrote the first if/elif chain's decision for every tanking team, every game — so even when the first chain legitimately decided "feature" for a tanking team's own isolation-scheme star, this second block always stomped it back to "conserve" — that's CA3. Fixing only one does nothing: without CA3, "feature" never survives to reach CA2's fix; without CA2, "feature" would survive but move zero points. Verified with a mandatory live smoke test driving the real production pipeline (`sim_orchestrator._sim_single_game`, real DB, no mocks except a `team_intel.compute_posture` monkeypatch to force a deterministic tanking posture) across 30 real games — pre-fix (via `git stash`): the star's directive was "conserve" in 30/30 games; post-fix: "feature" appears in some games, and those games average measurably more points for that player.

Other real bugs fixed in the same sweep: blowout garbage-time minutes never existed — starters played full minutes regardless of final margin, in either the coach-override or auto-allocate minutes branch (CA1, disclosed placeholder blowout thresholds); `bench_leash`/`transition_aggression` columns existed on `team_strategies` with real DB defaults and were settable via slash commands, but `strategy_repo.get_strategy`'s SELECT silently dropped both on every read (and `cpu_coach_service._load_human_gameplan` had the identical bug in its own inline query), so neither value had ever reached a real game for any team, human or CPU — no CPU coach ever set a real value for either field either, and `transition_aggression` was never converted from a label into an actual pace/turnover modifier (CA4); CPU coaches had zero scheme memory, re-rolling a fresh offensive/defensive scheme every single game despite `gameplan_repo.get_scheme_history` already existing for Coach Beat's columnist copy (CA5); a stale comment described the clutch-usage adjustment as a late-game-only mechanic when `sim_game` has no clock to target "late" usage against at all (CA6, cosmetic); `services/auto_strategy.py`'s archetype-inference code was confirmed dead (unreachable from the only real caller that passes `players=`, since that caller always also passes `override_strategy`) and deleted (CA7).

**Deferred, documented in `coaching-ai-logic-rules.md`, not built this pass**: CA8 (no foul-out mechanic — fouls are tracked but nothing reads the total; a real fix needs a two-pass minutes<->fouls resolution loop, not a scoped fix); CA9 (no same-game injury timing — a direct byproduct of the single-shot sim architecture; `sim_game` has no game clock to reorder a mid-game injury against); CA10 (flat defensive matchups — needs lineup-slot defensive-assignment tracking the engine doesn't have anywhere else, and is already self-documented as an accepted simplification in `_find_star_debuff_targets`'s own docstring).

Current test baseline: **793 passed, 1 skipped, 10 xfailed, 0 failed** (up from 780 passed pre-sweep; net +13 from this sweep's new tests, zero regressions). Verified via the same two-batch foreground split as the rollover sweep (see Traps) — batch 1 (`tests/test_a*.py`-`tests/test_maybe*.py` alphabetically, 42 files): 404 passed, 2 xfailed; batch 2 (remaining 43 files): 389 passed, 1 skipped, 8 xfailed.

## Traps

- **Background/long-running shell commands (`run_in_background: true`, or a foreground command that auto-moves to background after its timeout) got silently killed partway through in this session**, with no error surfaced beyond a bare "killed" status — happened 4 times in a row trying to background the full `pytest` run, at different completion percentages each time (not a fixed timeout), with zero relation to any actual test failure (all dots/expected xfails up to the kill point every time, confirmed via `pg_stat_activity` that no DB deadlock or stale connection was involved). **Workaround: run the suite as smaller synchronous foreground chunks instead** (e.g. split `tests/test_*.py` into two ~40-file batches, each run with `Bash`'s `timeout` param set high enough to complete, redirecting output to a file rather than piping through `tail` to avoid losing buffered output if a kill does happen). Root cause not identified — may be an environment/session-level constraint on background task lifetime, not specific to this project.
- **Running multiple `pytest` invocations concurrently against the shared local test Postgres (`localhost:5434/dba_test`) produces spurious cross-contamination failures**, not just the previously-documented alembic-version-drift issue. The autouse `clean_db` fixture's `TRUNCATE ... CASCADE` races across concurrent processes and throws `DeadlockDetectedError`/`ForeignKeyViolationError`/`UniqueViolationError` in modules that were never touched by whichever diff you're actually testing. Confirmed independently by 3 parallel builder agents this sweep via `pg_stat_activity`/`tasklist` process inspection — this is real DB contention, not a code regression. **Never trust a full-suite run's failure list as meaningful unless you've confirmed no other pytest process is running against the same DB** (`ps -W | grep python.exe`, or check for concurrent worktree agents) — rerun solo before treating any failure as real.
- **A `with (...)` block with too many parenthesized context managers can segfault Python 3.12 under pytest.** `tests/conftest.py`'s `patch_get_pool` fixture used one giant `with (patch(...), patch(...), ...)` statement; growing it past 18 entries (adding a 19th patch target) crashed the Python process outright — reproduced deterministically (3/3 clean without the extra line, 3/3 crash with it, content-independent — an unrelated dummy patch line crashed identically — and reproduced under both Bash and PowerShell). A standalone script with the identical `with` block ran fine, so this is specific to pytest's assertion-rewrite recompilation of the file combined with CPython 3.12's codegen for long parenthesized `with` statements. Fixed by refactoring to a target-list + `contextlib.ExitStack` loop (`_GET_POOL_PATCH_TARGETS`), which has no such ceiling. **If `patch_get_pool` ever needs another entry, add it to the list — do not revert to a parenthesized `with (...)` block.**
- **Shared `dba_test` Postgres DB's alembic version can silently drift when multiple worktree agents run pytest concurrently.** Each worktree's `conftest.py` runs `alembic upgrade head` from its own on-disk migration files against the one shared DB; if a worktree lacking a newer migration runs after one that has it, later test runs can find themselves below the migrations the merged code actually expects, producing confusing `KeyError`s on columns that "should" exist. Check `docker exec dba-db-1 psql -U dba -d dba_test -c "SELECT version_num FROM alembic_version;"` against `alembic heads` if a merge's tests fail with unexplained missing-column errors right after a multi-worktree session.
- **Docker Postgres (`dba-db-1`) shows real flakiness under concurrent/heavy test load** — deadlocks on the `clean_db` autouse fixture's `TRUNCATE ... CASCADE` colliding with in-flight test inserts, and the container itself crashed/restarted more than once during this session's work. Confirmed via `git stash` A/B testing by two independent agents that this is NOT caused by any code change — it reproduces on unmodified files too. A single sequential test run is clean. Worth a real look (e.g. faster volume, or serializing `clean_db`) if concurrent test runs against this DB become routine.
- **`_ROLE_EXPECTED_TENDENCIES` (the per-role tendency-threshold table used by trade eval's role-fit signal and now also by progression's role-fit compounding signal) is duplicated in `services/trade_narrative_lines.py`** — found by the progression role-fit agent, out of that agent's scope to fix. The canonical copy now lives in `services/trade_signals/role_fit.py` (progression imports its `classify_role_fit` helper from there); `trade_narrative_lines.py`'s copy is a separate, un-deduplicated literal. Worth consolidating in a future pass so the table has exactly one source of truth instead of two that can drift.
- **`docs/design/trade-logic-rules.md` now has a B9 rule** (roster-hole downweight, the audit's Trade #4) — read it before touching trade proposal/acceptance logic again; B8's status line was also corrected (it was already shipped, the doc previously said otherwise).
- **10 pre-existing test failures are quarantined** via `@pytest.mark.xfail(strict=False)` (`test_setup_cog.py` ×8, `test_cpu_trade_acceptance.py` ×2). A regression shows up as a NEW failure, not hidden inside these.
- **A per-margin/per-severity "0..1 factor" applied to a value that's then re-clamped to `[0, 1]` can silently erase a secondary multiplier's effect exactly at the extreme end of the range.** CA4 (coaching AI sweep) hit this: the first draft multiplied `bench_leash`'s severity factor onto the already-margin-clamped severity (`min(1.0, margin_factor) * leash_mult`, then re-clamped to 1.0) — at max blowout margin, `"short"` (1.3x) and `"normal"` (1.0x) both landed on the same clamped `1.0`, making the two leash settings indistinguishable exactly where a real coach's choice matters most. Fix: apply the secondary multiplier to the *downstream effect* (the reduction fraction), not to the already-clamped upstream factor, and clamp only the final result. Worth checking for this shape (`clamp(clamp(a) * b)`) anywhere else a "how severe" signal feeds a "how much extra" multiplier.

## Do not touch

- None currently.

## Recent context

- 2026-07-25 (latest): Shipped **realism sweep 5, Plan B (In-Game Strategy/Coaching AI)** — see "Next action" above for full detail. Single-agent sequential build (dependencies between CA2/CA3 ruled out a parallel-worktree split for this domain). CA1 (blowout garbage-time minutes), CA2+CA3 (usage-mode directive wiring + tanking-team usage bug, shipped together with a dedicated live smoke test), CA4 (bench_leash/transition_aggression wiring + missing-SELECT-column fix), CA5 (CPU scheme stickiness), CA6 (stale comment), CA7 (deleted dead auto-strategy code) all shipped; CA8/CA9/CA10 deferred as documented decisions. New `docs/design/coaching-ai-logic-rules.md`, which also retroactively documents a prior undocumented realism pass ("Finding #1"-"Finding #5") that predates this sweep. Full suite: 793 passed/1 skipped/10 xfailed (up from 780 pre-sweep, zero regressions), verified via the same two-batch foreground split as the rollover sweep. **This completes the four-domain realism sweep initiative begun with sweep 1 — there is no Plan C queued.**
- 2026-07-25 (earlier): Shipped **realism sweep 4, Plan A (Season Rollover)** — see git log / `docs/design/rollover-logic-rules.md` for full detail. 3 parallel isolated-worktree builder agents, merged sequentially with zero conflicts (core rollover pipeline: RO1/RO2/RO3/RO6/RO8/RO10; FA retirement age gate: RO4; roster-size floor: RO5). Full suite: 780 passed/1 skipped/10 xfailed, verified via two foreground batches after background pytest runs kept getting killed (see Traps).
- 2026-07-25 (earlier): Shipped **realism sweep 3** (playoffs/awards/HOF, rookie-to-roster minutes, progression role-fit — see "Next action" above for full detail). 3 parallel isolated-worktree builder agents (`eb90cd8` Domain D, `4d5bb59` D3, `b66a3ce` progression role-fit), merged sequentially (`3425768`, `54f41c8`, `99914d7`) with full-suite verification between each — all three touched fully disjoint file sets, confirmed during planning and again at merge time, zero conflicts. Followed with a dedicated mandatory live smoke test (`ba11b59`) for PA1 specifically, since a test that mocks every sub-dependency in isolation structurally cannot prove the real call site is wired — same lesson sweep 2's P1 (season off-by-one) already taught this project. Full plan: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`.
- 2026-07-25 (earlier): Shipped **realism sweep 2** (free agency, draft, progression/aging — see "Next action" above for full detail). 3 parallel isolated-worktree builder agents (`4b956ca` FA, `ad4891b` draft, `f091c91` progression), merged sequentially with full-suite verification between each. Post-merge review found and fixed 5 more real bugs the individual domain agents couldn't see in isolation (`9f46c9b`, `cb65cb8`, `6fc00bb`, `df7962b` — see Traps for the segfault and alembic-drift ones). Full plan: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`.
- 2026-07-24 (latest): Shipped the **B1 follow-up** (`a585176`) — wired `_assemble_index` to Keisha Williams, the first of the 5 Phase-1-revived `EmbedField` renderers to actually reach a live persona. Required extending `columnist_service.generate()`'s return contract additively: a new `_EMBED_RENDERERS` dict in `columnist_assembly.py` maps `format_style` -> EmbedData renderer, checked *before* `_assemble_article`; when a persona matches, `generate()` calls the renderer on the raw parsed dict and returns the result under a new `"embed_data"` key (present only for that persona — every other caller's dict is unchanged), flattening it via a new `_flatten_embed_data()` helper so the existing word-count/grounding-check/DB-storage logic needed zero changes. Keisha's `output_shape_override` now asks the LLM for real JSON fields (`metric_name`/`headline_value`/`definition`/`standouts`/`implication`) instead of a hand-formatted code-fenced string. `sim_content_pipeline.py`'s two live call sites for her categories (`game_recap`, `playoff_recap`) now share one `_embed_from_article()` helper that builds the embed from those fields when present. 8 new tests added (dispatch in `columnist_service`, the flatten helper in `columnist_assembly`, both call-site branches in `sim_content_pipeline`) — zero existing tests needed changes. Verified independently: ruff clean, full suite 666 passed/1 skipped/10 xfailed. Also had to `docker start dba-db-1` after a machine reboot left it `Exited` — the container doesn't restart with Docker Desktop automatically.
- 2026-07-24 (earlier still): Shipped columnist overhaul **Phase 2** (per-persona content passes) — split into two parallel worktree agents by file ownership rather than the plan's original category grouping, after finding several items shared files in ways that would've collided (the per-persona word-target table in `columnist_service.py`, and Big Picture/Marcus Cole both needing output-shape changes that overlapped with the shared rendering plumbing). **Group A** (`d2db86c`, merged `--no-ff`): Pat Chen ships the previously-scoped-but-never-shipped ≤120-word hard cap plus player-detail rules (name the defender who got cooked, name the teammate who benefited), and a conditional history rule (real historical comparisons allowed when `all_time_records` data actually supports one, season-1 humility otherwise). Coach Beat gets a tightened length cap and a new scheme-history awareness (has this team run this defensive/offensive look before, did it work — new `gameplan_repo.py` query + `columnist_intel` provider). Marcus Brooks and Ren Takahashi — the two thinnest personas — get real analytical mechanics. Darius Cole gets real draft/prospect scouting substance. Power List and The Ledger get season-transition callbacks gated on real `season_history` data. **Group B** (`021d792`, merged `--no-ff`, one adjacent-line conflict in `_PERSONA_WORD_TARGET` resolved cleanly): Big Picture's word target now actually matches its own stated char cap (was internally inconsistent), gains real season-history/HOF callbacks, and gets a "Read full column" expand button (`bot/ui/big_picture_view.py`, new — short teaser by default, full column behind a button). Marcus Cole's trade reports reference real trade-magnitude rankings and split into two embeds (reporter-take summary + structured asset-breakdown grid) instead of one crammed description. Both agents got killed mid-run by a monthly Claude API spend cap partway through — Group A had already finished cleanly by then; Group B was resumed via `SendMessage` (not restarted fresh) once the user raised the limit, preserving its in-progress context. Independently verified both post-merge exactly like Phase 1 (ruff clean, full suite fresh in each worktree before merging, one more full run on `master` after both landed — 658 passed/1 skipped/10 xfailed, zero regressions). Full plan: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`.
- 2026-07-24 (earlier yet): Shipped columnist overhaul **Phase 1** (`1b2a1e9`, merged from an isolated worktree, `--no-ff`) — foundational infra for the concision/layout/history-flavor request that came out of watching the realism audit live. Per-format `max_tokens` budgets replacing the flat 1400 ceiling, `narrative_rule` scoped to long-form categories only, code-level length enforcement with a regenerate-once retry (mirrors the fact-check retry shape), tightened Discord description truncation (was a flat 2000-char slice), 5 previously-dead renderers revived as `EmbedField`-based layouts, 5 list-style personas (Power List/The Ledger/The Race/Triage Report/Rookie Watch) converted to multi-field embeds, a trade-magnitude comparator (`services/trade_magnitude.py`, new file), and 3 new `columnist_intel` providers (`season_history`/`hall_of_fame`/`all_time_records`, zero new schema). Independently verified post-merge (not just the agent's self-report): `ruff check` clean on all 7 touched files, full suite run fresh in the worktree — 625 passed/1 skipped/10 xfailed (636 collected, 48 new tests), then re-run on `master` post-merge with the same result modulo one confirmed-flaky unrelated test (`test_progression.py::test_high_potential_grows_more`, passes standalone, not touched by this work). Full plan: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md`.
- 2026-07-24: Shipped a full realism/immersion audit — 23 findings across 3 domains, each researched by a dedicated Explore agent then fixed by a dedicated builder agent in an isolated git worktree (parallel dispatch, sequential merge, zero conflicts since domains touched disjoint files). **Trades** (11 fixes, `3ade9c3`): B6 archetype-redundancy now checked on trade *accept* not just search, positional-need gating for non-contenders, roster-shape floor, a new B9 roster-hole downweight rule, B3 upside modifier now reaches grading, season-long trade-partner cooldown, controlled variety in mode selection, star-power value premium, de-duplicated B1 want-check logic, `is_cornerstone` now uses live posture. **Scheme/personnel fit** (5 fixes, `fec6882`): CPU coaching posture now reads `franchise_plan.goal` (ports the B7 fix that only covered trades before), defensive scheme selection (press/switch_all) now gated on the team's own roster speed/defense, scheme tendency bumps now skill-conditioned per player instead of flat, roles and schemes now cross-reference (two-sided synergy bonus/penalty, no more forcing a center into a ball-handler role), bench role assignment nudges away from duplicate archetypes. **Columnist content** (7 fixes, `f5a5a84`): LLM-generated articles get a post-generation grounding check against real context data, player-level "declining/washed" claims are now grounded in real form data (reused the trade system's `compute_form_map`), Hot Take Hour's season narratives re-validate against current standings instead of freezing forever, Darius Cole's lottery odds are computed from real standings gaps instead of a hardcoded array, power rankings store structured rank data instead of regex-parsing prose, rookie stats are recency-weighted, and a documented-but-never-shipped Pat Chen prompt fix finally shipped. Full plan/findings detail: `C:\Users\Owner\.claude\plans\swirling-stargazing-rabin.md` (outside this repo).
- 2026-07-24 (earlier): closed out the prior re-architecture sweep fully — Discord intent toggle and all 3 trade verifications turned out already resolved, `dba-site` command reference pushed live, `sim_orchestrator.py` split shipped. See git log for that work.
