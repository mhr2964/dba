# Design notes — DBA

Durable design/architecture docs for this project, committed to the repo so they travel with the code (distinct from `HANDOFF.md`, which is transient work-stream state, and from this assistant's own `Brain/Note Pad/dba/`, which is workspace-level AI memory that isn't part of this repo).

| File | Covers | Status |
| --- | --- | --- |
| [architecture.md](./architecture.md) | Layered-monolith structure (`bot/`/`services/`/`data/`/`core/`/`phase/`); the `services/` never imports `discord`, `data/repositories/` owns all SQL invariant; `Announcer` protocol | Current |
| [trade-logic-rules.md](./trade-logic-rules.md) | CPU trade evaluator + proposal generator rule specs (B1-B8): posture gating, archetype/role fit, asset upside valuation, safety-gate parity across incoming/outgoing-first paths | Current |
| [trade-proposal-restructure.md](./trade-proposal-restructure.md) | Swap-aware bidirectional trade initiation (incoming-first vs outgoing-first modes), `pick_proposal_modes` dispatcher | Shipped 2026-05-22 |
| [columnist-ride-along.md](./columnist-ride-along.md) | Attach-only sidecar + file-IPC design for pausing sims to capture live feedback on columnist articles mid-run | Shipped 2026-05-21 — superseded in practice by the Discord-reply feedback capture feature (`ae7cd97`), which coexists with the sidecar rather than replacing it; see `HANDOFF.md` |
| [persona-redesign.md](./persona-redesign.md) | 8-persona voice/renderer redesign — headline-dedup defense-in-depth, cadence trigger changes | Current — drafted, not yet implemented |
| [playoffs-awards-hof-logic-rules.md](./playoffs-awards-hof-logic-rules.md) | Playoff sim-path realism (PA1's pre-sim extraction shared with the regular season, Finals home court, series-MVP scoping), CPU awards voting (MVP formula unification, DPOY/POTM/voter-profile fixes), HOF induction (All-NBA/All-Star path, retirement/age gate) rule specs (PA1-PA14) | Current |
| [rollover-logic-rules.md](./rollover-logic-rules.md) | Season-to-season transition pipeline realism (contract extension ordering, standings history retention, HOF induction timing, FA-side retirement age gate, roster-size floor at season start, salary cap growth) rule specs (RO1-RO10) | Current |
| [coaching-ai-logic-rules.md](./coaching-ai-logic-rules.md) | In-game strategy/coaching AI realism (blowout garbage-time minutes, usage-mode directive wiring + tanking-team usage bug, bench_leash/transition_aggression wiring, CPU scheme stickiness), plus retroactively-documented prior findings (personnel gating, skill-conditioned scheme magnitudes, scheme-synergy penalty, role routing, posture-source unification) rule specs (CA1-CA10) | Current |
| [franchise-plan-logic-rules.md](./franchise-plan-logic-rules.md) | CPU franchise strategic-direction derivation realism (reassessment/pivot checkpoint cadence fixed to fire every sim sub-batch instead of once per top-level call, derive-failure observability, real-DB test coverage) rule specs (FP1-FP4) | Current |
| [role-assignment-logic-rules.md](./role-assignment-logic-rules.md) | Per-player role assignment realism (FA/waiver "roster ghost" fix mirroring the draft path, small-roster primary-scorer/defensive-anchor guarantee, `/coach role show` display renormalization, real-DB test coverage) rule specs (RA1-RA4) | Current |
| [phase-transition-logic-rules.md](./phase-transition-logic-rules.md) | League phase state-machine realism (`advance_phase` validated against a real phase-adjacency graph instead of a blind UPDATE, `/league advance` clear rejection message, rollover's phase write routed through the same gate, sim-batch season-complete hook re-checks phase before advancing, real-DB test coverage) rule specs (PT1-PT5) | Current |
| [trade-block-logic-rules.md](./trade-block-logic-rules.md) | CPU trade-block listing realism (candidate-selection branch driven by live `team_intel` posture instead of the stale creation-time `cpu_mode` column; `asking_price` units-consistency fix — removed the lone `player_trade_value * 1_000_000` dollar scaling found nowhere else in the trade-value call graph) rule specs (TB1-TB2) | Current |

Shipped docs are kept as historical design record — they explain *why* a shipped feature is shaped the way it is, which git log alone doesn't answer well. Update a doc in place if its subject's architecture changes again; don't append a new dated entry to the same file (that's what git history is for).
